# Local PostgreSQL (docker-compose up -d)
# DATABASE_URL="postgresql://navigator:navigator@localhost:5433/navigator_tour?schema=public"

# Supabase (production / Vercel) — use transaction pooler (6543), not session (5432)
DATABASE_URL="postgresql://prisma_app.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1"
NEXT_PUBLIC_SUPABASE_URL="https://nynbxynddxxevkcsuuog.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="eyJ..."

AUTH_SECRET="generate-a-random-secret-here"
